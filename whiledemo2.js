var i = 3;
//we can write while(i!=0)as simply while(i)
//as any value be converted to boolean equivalent
while (i) { // when i becomes 0, the condition becomes false, and the loop stops
     console.log(i);
     i--;
}
