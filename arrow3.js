var func1 = (num1,num2) => {
    return num1 + num2; //Since curly braces are used
    //if we don't use return statement the this arrow 
    //function simply returns undefined
}

console.log(func1(4,5));
